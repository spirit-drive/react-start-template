import type { Meta } from "@storybook/react";
import { ModalWithInputButton } from "./ModalWithInputButton";

const meta: Meta<typeof ModalWithInputButton> = {
  title: "Компоненты/Buttons/ModalWithInputButton",
  component: ModalWithInputButton,
  tags: ["autodocs"],
};

export default meta;

export const modalWithInputButton = {};