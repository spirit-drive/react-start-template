import type { Meta } from "@storybook/react";
import { LangSwitcher } from "./LangSwitcher";

const meta: Meta<typeof LangSwitcher> = {
  title: "Компоненты/Switchers/LangSwitcher",
  component: LangSwitcher,
  tags: ["autodocs"],
};

export default meta;

export const langSwitcher = {};