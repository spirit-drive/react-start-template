import type { Meta } from '@storybook/react';
import { EShop } from './EShop';

const meta: Meta<typeof EShop> = {
  title: 'Компоненты/Apps/E-Shop',
  component: EShop,
  tags: ['autodocs'],
};

export default meta;

export const eshop = {};
