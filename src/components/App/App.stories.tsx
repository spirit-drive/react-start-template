import type { Meta } from '@storybook/react';
import App from './App';

const meta: Meta<typeof App> = {
  title: 'Компоненты/App',
  component: App,
  tags: ['autodocs'],
};

export default meta;

export const app = {};
