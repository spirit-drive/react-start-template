import type { Meta } from "@storybook/react";
import { CartBlock } from "./CartBlock";

const meta: Meta<typeof CartBlock> = {
  title: "Компоненты/CartBlock",
  component: CartBlock,
  tags: ["autodocs"],
};

export default meta;

export const cartBlock = {};